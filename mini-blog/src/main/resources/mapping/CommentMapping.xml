<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ankypas.miniblog.Mapper.CommentMapper">
    <resultMap id="commentMap" type="ankypas.miniblog.Dao.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="blogId" column="blog_id"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="userId" column="user_id"/>
        <result property="text" column="text"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
    <insert id="add" parameterType="ankypas.miniblog.Dao.Comment">
        insert into comment(blog_id,parent_comment_id,user_id,text) values(#{blogId},#{parentCommentId},#{userId},#{text})
    </insert>
    <select id="countByCommentIdAndUserId" parameterType="int" resultType="int">
        select count(*) from comment where comment_id = #{commentId} and user_id = #{userId}
    </select>
    <select id="countByCommentId" parameterType="int" resultType="int">
        select count(*) from comment where comment_id = #{commentId}
    </select>
    <delete id="delete" parameterType="int">
        delete from comment where comment_id = #{commentId}
    </delete>
    <update id="update">
    </update>
    <select id="find" parameterType="int" resultMap="commentMap" resultType="ankypas.miniblog.Dao.Comment">
        select * from comment where blog_id = #{blogId}
    </select>
</mapper>
